# ===================================================================
# Spring Boot configuration for the "dev" profile.
#
# This configuration overrides the application.yml file.
# ===================================================================
logging:
  level:
    ROOT: INFO
    com.zensar.api: INFO

spring:
  profiles:
    active: test
  jackson:
    serialization:
      indent-output: true
server:
  port: 8080

application:
  app-backend: "http://localhost:1881"